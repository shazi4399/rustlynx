use std::error::Error;
use super::super::super::super::Context;
use super::extra_trees::XTContext;
use super::super::decision_tree::TrainingContext;

pub fn run(ctx: &mut Context) -> Result<(), Box<dyn Error>> {

    Ok(())
}

